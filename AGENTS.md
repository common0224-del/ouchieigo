# オウチエイゴ開発ルール

このファイルは、このGitリポジトリで作業するCodexに適用する。対象は4〜6歳の子どもで、連打・誤操作が日常的に起きる。SafariとChromeで使うWebアプリである。

## 画像の標準（所有者の明示的な指示なしに変更しない）

- 公開アプリから参照する画像はWebP形式に統一する。新規画像と差し替え画像の両方に適用する。
- 背景画像は **900×1200px以内**、アイテム・届け先画像は **長辺512px以内**、Action Matchの絵は **長辺800px以内**。
- 画質設定は **WebP quality 84**。今回の画像最適化と同じ値を使う。
- 元のPNG/JPGはMac上の `source-images/` に置き、`./scripts/optimize-images.sh` を通してから使う。元画像を公開アプリの参照先やGitの追跡対象に置かない。
- Action Match の人物画像は、承認済みの人物基準画像を参照して **緑 #00FF00 の単色背景**（透過や市松模様を生成させない）で制作する。`scripts/chroma-key-green.py` で緑をアルファ透過に変換し、輪郭に緑のふちが残っていないか淡色と青色の背景に重ねて拡大確認する。WebP quality 84・長辺800px以内で出力し、`tests/check-image-alpha.mjs` で四隅と透明部分を検査してから公開する。緑色の小道具や服を描く場合はこのキー方式をそのまま適用せず、所有者へ相談する。
- 現在の3カテゴリに当てはまらない画像の追加・更新では、上限値を独断で決めない。所有者へ確認する。既存のスプライトなどの例外を更新するときも確認する。
- 上限値や品質を見直す必要があれば、高解像度端末での画質不足などの根拠と新しい値を先に提案する。承認前に値を変更しない。
- 画像を追加・変更したら、変換前後の各ファイルサイズと削減率、**全WebP画像の合計容量**、**HTMLとWebPを含む配布用アプリ本体の合計容量**を報告する。
- 配布用アプリ本体は **10MiB以内**を目安とし、超えそうなら追加を公開する前に所有者へ相談する。
- 元画像のバックアップはTime Machineまたは暗号化された外付けドライブを推奨する。`source-images/` は `.gitignore` で追跡しない。ZIPにも含めない。

最適化前は、現在の画面が参照する60画像だけで約79.8MBあり、iPhone実機で数秒の読み込み遅延が起きた。PNGの背景は1枚約2MBで、WebP化で約95%削減できた。所有者の確認後、未使用WebP42枚（約3.19MiB）を削除した。2026-09-24時点の全WebP60枚は2,458,494 bytes（約2.35MiB）、`index.html`・`speech-test.html`・全WebPは2,621,589 bytes（約2.50MiB）である。以前報告したアプリ本体約6.31MiBには保管用の過去HTMLも含まれていた。

## 音声

- 読み上げは必ず共通の `playSpeech()` を経由する。場面ごとに同じ発話処理をコピーしない。
- ゲーム進行を音声合成の `onstart` / `onend` / `onerror` に依存させない。音声が無音・失敗・停止しても、アイテム操作、次問選択、完了表示は継続させる。
- 読み上げイベントの後に別の処理を行う必要がある場合は、イベントが発火しなくても必ず先へ進むタイムアウトを設ける。
- SafariとChromeで挙動が異なる処理はブラウザ判定でガードする。両方の正常動作を検証する。
- `Samantha` などの標準的な英語音声を優先し、`Albert` などのノベルティ音声を選ばない。
- Chromeでは `speechSynthesis` のキューが詰まるとブラウザ再起動まで無音になることがある。連打による `cancel()` と `speak()` の高頻度な往復を避ける。

## コード・ゲーム内容

- 新しい画面やモックの着手前に、既存の洗面所・リビング・寝室・Word Match のHTML/CSSと使用素材を読む。配色変数、部屋の背景、猫マスコット、ヘッダー、白フチ・下方向の影・押下時の沈み込みを確認し、初稿から同じ世界観に揃える。絵文字をキャラクターや場面絵の代用にしない。
- 未採用の試作・検証用HTMLは、Gitが追跡しない `private-prototypes/` に置く。公開ルートへ直接作成・移動しない。採用時は必要な内容だけ `index.html` へ統合し、公開対象を明示してからコミットする。
- 将来の配布は全画面アプリを前提とし、ビューポート高は `100dvh` を基本にする。`env(safe-area-inset-top)` と `env(safe-area-inset-bottom)` を常に考慮する。
- 基準端末はiPhone縦向きとし、最低でも 375×667 / 390×844 / 430×932 で確認する。横向きは現段階の保証対象外。
- 共通ヘッダーは画面上部に固定し、画面遷移してもスクロールで消えない構造にする。通常状態で意図しない縦スクロールを発生させない。
- ヘッダーは共通部品だけを使い、画面ごとに独自のヘッダーを作らない。
- 子どもが操作するボタン、カード、ドラッグ対象は最小48pxを確保する。字幕・トレー・カード・主要操作を重ねず、画面外へはみ出させない。
- キャラクター素材は同じ役割・年齢・服装・季節で統一する。新規キャラクター素材を作る前に既存素材を並べて比較し、流用できるものと新規作成が必要なものを提案して所有者の確認を得る。
- 人物を描く画像は、完成後の `docs/characters.md` の設定表に従う。
- 人物が登場する画像は `docs/image-prompts.md` の固定 `[STYLE]`・該当人物の `[CHARACTERS]`・`[DO NOT]` をそのまま使い、登場人物の `docs/characters/` 基準画像を必ず添付する。変更できるのは場面固有の `[SCENE]` と用途に合う `[TECHNICAL]` の選択だけ。固定ブロックや未承認の服を独断で変えない。
- 画像制作前に、アプリが上に重ねる三角・枠線・ボタン・ラベル・吹き出し枠の有無を確認する。重ねる位置・画面上の大きさを `[SCENE]` の `Composition` に明記し、顔・手・動作の対象物をその位置に置かない。新しい画面・画像種別では重ね表示がない場合も報告する。Think & Say の絵・カードにも適用する。
- Action Match の新規画像は等幅のコマを左から右に並べ、境界周囲に余白を設け、区切り線と矢印を描き込まない。三角はアプリ側で重ねる。重ね表示を追加・変更するときは、既存画像との衝突を実際のカード・画面で確認してから適用する。
- 生成後は基準画像と並べた比較画像および実際のカード／場面表示を確認・報告し、採用画像ごとにプロンプト全文と添付基準画像を `docs/image-log.md` に記録する。人物のずれが再発したら、`[DO NOT]` への追記案を所有者に提案する。

- 洗面所、朝ごはん、リビング、寝室の4場面で同じ処理を複製しない。新しい場面は既存場面をコピーせず、共通関数にデータを渡して追加する。
- 既存の関数・変数・CSSルールを修正するときは元の定義を置き換える。同名の定義を下へ書き足さない。変更後は `tests/check-index-syntax.mjs` で構文と重複定義を検査する。
- `current`・`task` などが `null` になり得る箇所は、操作開始時と完了時の両方でガードする。朝ごはんだけガード漏れでクラッシュしたことがある。
- 各タスクの英文 `phrases` と日本語キャプションは、所有者の指示なしに変更しない。
- 新しい場所や届け先を作る場合は、アプリの世界観に合うイラストを用意し、Word Matchにも組み込む。
- Word Matchのカードは同一サイズ・同一構造を保ち、追加時に実際のDOMと重複カードを確認する。モード選択でItem Matchカードが描画されなかったことがある。
- アイテムの補充、判定範囲、字幕、全問終了後の操作を4場面で確認する。正解後の補充漏れや判定範囲の重複が過去に起きている。
- ドロップ判定枠は互いに重ねない。見た目より広めの判定範囲より、正しい場所で誤判定しないことを優先する。
- ドロップ後のアイテムは必ず「完了」または「元のトレースロットへ復帰」のどちらかにし、宙に残る第三の状態を作らない。

## 確認と配布

- 変更後は4場面すべてを確認し、1場面だけの結果で完了としない。SafariとChromeの両方を確認する。実機・ブラウザを操作できない場合は、未確認と明記する。
- ゲーム進行を変更した場合は、正解・不正解・枠外・中断を含む自動テストで4場面すべてが最後まで完了し、途中で掴めるアイテムがゼロの進行不能状態にならないことを確認する。
- 画像を含む変更では低速回線でも画像読み込み前後の場面サイズが安定することを確認する。
- 元画像と過去版HTMLを配布用ZIPから除外する。GitHub Pagesに置くファイルは公開され得るため、ライセンス、公開範囲、ファイルサイズを確認する。将来の有料販売を考慮する。
- 未使用画像42枚は所有者の承認で削除済み。元画像は `source-images/unused/` に保管され、通常の最適化コマンドでは再生成されない。削除記録は `UNUSED-IMAGES.md` を参照する。
